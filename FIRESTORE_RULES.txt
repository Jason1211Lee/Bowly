// Firestore 安全規則
// 在 Firebase Console > Firestore Database > Rules 中設定此規則

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 只允許已登入的用戶存取他們自己的記錄
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
