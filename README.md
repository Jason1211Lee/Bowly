# Bowly 🎳 - 保齡球戰績追蹤平台

> 一個現代化的保齡球成績記錄和分析平台，幫助你追蹤進度、達成目標。

## 🎯 功能特性

✅ **帳號認證** - Firebase 登入/註冊，跨裝置同步戰績
✅ **戰績輸入** - 快速記錄每場比賽的分數、全倒、補中數據
✅ **即時統計** - 自動計算平均分、最高分、場次、目標差距
✅ **數據可視化** - 分數趨勢折線圖、全倒/補中分佈圖
✅ **目標追蹤** - 設定並追蹤目標平均分進度
✅ **AI 建議** - 根據成績生成訓練建議
✅ **雲端同步** - 使用 Firebase Firestore，自動同步多裝置資料
✅ **數據管理** - 匯出/匯入 JSON 備份、刪除管理
✅ **響應式設計** - 完美支持手機、平板、桌機

## 🚀 快速開始

### 前置要求

- Node.js >= 14.0.0
- npm 或 yarn

### 安裝步驟

1. **clone或下載項目**
   ```bash
   git clone https://github.com/Jason1211Lee/Bowly.git
   cd Bowly
   ```

2. **安裝依賴**
   ```bash
   npm install
   ```

3. **啟動應用**
   
   **開發模式（本地運行）**：
   ```bash
   npm start
   ```
   
   應用將在 `http://localhost:3000` 啟動
   
   瀏覽器會自動打開，如果沒有請手動訪問上述地址

4. **停止應用**
   
   在終端中按 `Ctrl+C` 停止服務器

## 📁 項目結構

```
Bowly/
├── public/                  # 前端應用文件
│   ├── index.html          # 主頁面
│   ├── app.js              # 核心邏輯
│   └── style.css           # 樣式文件
├── server.js               # Express 伺服器
├── package.json            # 依賴和腳本
├── .env                    # 環境配置（本地）
├── .env.example            # 環境配置模板
├── .github/
│   └── copilot-instructions.md  # AI 開發指南
└── README.md               # 本文件
```

## 🔧 配置文件

### .env 環境變數

編輯 `.env` 文件可配置以下項目：

```bash
# 伺服器設定
PORT=3000                    # 運行端口（默認 3000）
HOST=localhost              # 運行主機（默認 localhost）

# CORS 設定
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 應用環境
NODE_ENV=development        # 開發或生產
```

**本地開發**：保持默認設定即可
**線上部署**：根據你的域名修改 ALLOWED_ORIGINS

## 💻 使用指南

### 帳號登入 / 註冊

1. 訪問應用首頁
2. 選擇「登入」或「註冊」頁籤
3. **登入**：輸入已註冊的電子郵件和密碼
4. **註冊**：輸入電子郵件、設定密碼並確認
5. 登入成功後，所有戰績將自動同步到 Firebase Firestore

### 帳號管理

- **跨裝置同步**：登入同一帳號即可在任何裝置上存取你的所有戰績
- **安全退出**：點擊導航欄的「登出」按鈕

### 添加戰績

1. 填入**比賽日期**和**總分**（必填）
2. 輸入**全倒數**和**補中數**（可選，默認為 0）
3. 或使用「逐格輸入」按鈕，透過虛擬鍵盤輸入詳細的保齡球記分卡
4. 點擊「新增紀錄」按鈕

### 逐格輸入 (Frame-by-Frame Scoring)

- **第 1-9 框**：每框最多兩次投球
  - 輸入 `X` 表示全倒（Strike）
  - 輸入 `/` 表示補中（Spare）
  - 輸入 `0-9` 表示擊倒的瓶數
- **第 10 框**：最多三次投球（若有全倒或補中）
- **虛擬鍵盤**：點擊按鈕或使用物理鍵盤輸入

1. 在目標設定區輸入你的**目標平均分**（例如 170）
2. 點擊「保存目標」
3. 統計區會實時顯示與目標的差距

### 數據管理

- **📥 匯出 JSON**：下載所有戰績數據為 JSON 文件備份
- **📤 匯入 JSON**：上傳之前導出的 JSON 文件恢復數據
- **🗑️ 清空全部**：刪除所有記錄（需要確認）
- **刪除單筆**：在完整紀錄列表中點擊刪除按鈕

## ⚙️ Firebase 設定

此應用使用 Firebase 進行帳號認證和雲端數據儲存。

### 設定 Firestore 安全規則

請在 Firebase Console 中設定以下安全規則以保護用戶數據：

**Firebase Console > Firestore Database > Rules**

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 只允許已登入的用戶存取他們自己的記錄
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 環境設定

Firebase Config 已內建於應用，無需額外設定。

## 🌐 線上部署指南

### 部署到 Vercel（推薦，免費）

1. **推送代碼到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/Bowly.git
   git branch -M main
   git push -u origin main
   ```

2. **連接到 Vercel**
   - 訪問 https://vercel.com
   - 登入或註冊（使用 GitHub 賬號）
   - 點擊「New Project」
   - 導入你的 GitHub 倉庫

3. **配置環境變數**
   - 在 Vercel 項目設定中添加以下變數：
   ```
   PORT=3000
   NODE_ENV=production
   ALLOWED_ORIGINS=https://yourdomain.vercel.app
   ```

4. **部署完成**
   - Vercel 會自動構建和部署
   - 你的應用將在 `https://yourdomain.vercel.app` 上線

### 部署到 Heroku

1. **安裝 Heroku CLI**
   ```bash
   npm install -g heroku
   heroku login
   ```

2. **創建 Heroku 應用**
   ```bash
   heroku create your-app-name
   ```

3. **設定環境變數**
   ```bash
   heroku config:set NODE_ENV=production
   heroku config:set ALLOWED_ORIGINS=https://your-app-name.herokuapp.com
   ```

4. **部署應用**
   ```bash
   git push heroku main
   ```

### 部署到自有服務器

1. **購買服務器**（VPS、雲主機等）
2. **安裝 Node.js 和 npm**
3. **克隆項目並安裝依賴**
   ```bash
   git clone https://github.com/Jason1211Lee/Bowly.git
   cd Bowly
   npm install --production
   ```
4. **配置反向代理**（使用 Nginx）
   ```nginx
   server {
     listen 80;
     server_name yourdomain.com;
   
     location / {
       proxy_pass http://localhost:3000;
       proxy_http_version 1.1;
       proxy_set_header Upgrade $http_upgrade;
       proxy_set_header Connection 'upgrade';
       proxy_set_header Host $host;
       proxy_cache_bypass $http_upgrade;
     }
   }
   ```
5. **使用 PM2 管理進程**
   ```bash
   npm install -g pm2
   pm2 start server.js --name "bowly"
   pm2 startup
   pm2 save
   ```

## 🎨 自訂樣式

編輯 `public/style.css` 可自訂應用外觀：

- **色系變數**：在 `:root` 中修改
- **字體**：修改 `body` 的 `font-family`
- **主題色**：修改 Bootstrap 色系類名

## 📊 數據格式

所有數據保存在瀏覽器 localStorage 中，格式如下：

```json
{
  "records": [
    {
      "id": "id12345",
      "date": "2025-12-10",
      "score": 185,
      "strikes": 8,
      "spares": 5,
      "createdAt": 1734000000000
    }
  ],
  "target": 170
}
```

## 🔐 安全性注意

- ⚠️ 所有數據存儲在瀏覽器本地，不會上傳到服務器
- 💾 定期匯出 JSON 備份你的數據
- 🧹 清除瀏覽器緩存會刪除本地數據

## 🚧 已知限制

1. 每日僅支持一筆成績記錄（同日期會提示覆蓋）
2. 數據僅存儲在當前瀏覽器
3. 多裝置同步需通過 JSON 匯出/匯入

## 📈 未來改進

- [ ] Firebase 雲端同步
- [ ] 用戶認證系統
- [ ] 分享功能
- [ ] 更高級的統計分析
- [ ] 移動應用版本

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

1. Fork 本倉庫
2. 創建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

## 📄 開源許可

本項目採用 MIT License - 詳見 [LICENSE](LICENSE) 文件

## 📞 技術支持

- 📧 Email: jason1211.lee@gmail.com
- 💬 GitHub Issues: https://github.com/Jason1211Lee/Bowly/issues
- 📖 Wiki: https://github.com/Jason1211Lee/Bowly/wiki

## 🙏 致謝

感謝所有使用者的支持！

---

**Bowly** - 讓你的保齡球之路更精彩 🎳
